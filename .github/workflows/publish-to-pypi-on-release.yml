name: Publish to PyPi on Release

on:
  push:
    branches: [ main, master ]
  pull_request:
  release:
    types: [ published, edited ]  # fires when you publish or edit a GitHub Release

jobs:
  build-and-publish:
    # needs: test
    runs-on: ubuntu-latest
    # Only run on GitHub Release events that are NOT prereleases
    if: github.event_name == 'release' && github.event.release.prerelease == false
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # hatch-vcs needs tags
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Debug git state
        run: |
          git describe --tags --dirty
          git log --oneline -5
          ls -la src/quickslurm/
      - name: Build sdist/wheel
        run: |
          python -m pip install --upgrade pip
          pip install build hatchling hatch-vcs
          python -m build
      - name: Check version file
        run: |
          ls -la src/quickslurm/
          find . -name "_version.py" -exec cat {} \;
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@v1.10.3
        with:
          password: ${{ secrets.PYPI_TOKEN }}
